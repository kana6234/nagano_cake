<%- if controller_name != 'sessions' %>
  <h5 class="d-inline-block bg-light my-4 px-3">既に登録済みの方</h5>
  <div class="ml-5">
    <u><%= link_to "こちら", new_session_path(resource_name) %></u> からログインしてください
  </div>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <div class="row">
    <div class="col ml-5">
      <p class="d-inline-block bg-light my-4 px-3">既に登録済みでない方</p>
    </div>
  </div>
  <div class="row">
    <div class="col ml-sm-5 px-5">
      <u><%= link_to "こちら", new_registration_path(resource_name) %></u>　から新規登録を行なってください。
    </div>
    </div>
<% end %>